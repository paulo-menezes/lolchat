{
  "name": "lolchat-server",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^5.7.11",
    "mongoose-timestamp": "^0.6.0",
    "websocket": "^1.0.30"
  },
  "scripts": {
    "prod": "npm i && cd client && npm run build && cd .. && node server.js",
    "preinstall": "cd client && npm i"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
